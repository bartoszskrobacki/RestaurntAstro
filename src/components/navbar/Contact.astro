---
import Icon from '@components/shared/Icon.astro'

export interface Props {
	phoneNumber: string
}

const { phoneNumber } = Astro.props
---

<a class="hidden md:flex" href=`tel:${phoneNumber}`
	><Icon icon="phone" /> <span class="pl-1 font-bold">{phoneNumber}</span></a
>
